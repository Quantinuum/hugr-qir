### Lowering pass examples

This is a collection of qir/llvm examples containing qubit pointer ssa variables generated through select and phi instructions.

They are for testing the lowering passes and are not valid qir in general.
